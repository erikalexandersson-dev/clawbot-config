# 2026-03-09

- Patched `/root/.openclaw/openclaw.json` to add Anton Alexandersson’s Telegram ID (`46730753117`) to both `channels.telegram.allowFrom` and `groupAllowFrom`, then restarted the gateway. Telegram still returns `chat not found` because Anton hasn’t messaged the bot post-allowlisting; waiting for him to send a new /start or “hej” so the DM thread opens and I can deliver Erik’s “hej”.
- Erik requested adding Karin, Tilde, and JP to the Telegram allowlist, but I still need their actual Telegram chat IDs (phone numbers aren’t sufficient); asked Erik to forward real messages from each or have them ping the bot so I can capture the IDs before patching config and restarting again.
