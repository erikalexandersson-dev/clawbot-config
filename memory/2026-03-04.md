
## 2026-03-04
- Glooko automation: successfully logged in to eu.my.glooko.com (Anton Alexandersson) via OpenClaw browser profile `erik`; Summary/Graphs accessible.
- Telegram group access fixed for new group chat "Diabeteschatt med Al" (chatId -5038834189) after logs showed `reason: not-allowed`; configured `channels.telegram.groupAllowFrom=["5673904671"]` and group override `requireMention:false`.
- User preference update: Erik explicitly wants passwords stored locally in VM secrets by default unless he says otherwise (still never accept passwords via Telegram/chat; capture them via local VM prompt).
- Stored Glooko credentials locally on VM in `/root/.openclaw/secrets.env` (root-only) as `GLOOKO_EMAIL` + `GLOOKO_PASSWORD` (added alongside existing ForwardEmail creds).
- Config change requested: add message tool cross-context markers per JP suggestion. Updated `/root/.openclaw/openclaw.json` under `agents.defaults.tools.message.crossContext` with `allowAcrossProviders:true` and marker prefix `[from {channel}] `; attempted `openclaw gateway restart` afterwards.
